---
name: {{SERVICE_NAME}}
description: {{DESCRIPTION}}
metadata:
  author: {{AUTHOR}}
  version: "0.1.0"
  category: {{CATEGORY}}
  tags: "{{TAGS}}"
  complexity: standard
license: {{LICENSE}}
allowed-tools: Bash(python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py *)
---

# {{SKILL_NAME}}

{{DESCRIPTION}}

## Installation

1. **Install Python dependencies**:
   ```bash
   pip install --user requests keyring pyyaml
   ```

2. **Download the skill** from [Releases](https://github.com/odyssey4me/agent-skills/releases) or use directly from this repository.

## Setup Verification

After installation, verify the skill is properly configured:

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py check
```

This will check:
- Python dependencies (requests, keyring, pyyaml)
- Authentication configuration
- Connectivity to {{SKILL_NAME}}

If anything is missing, the check command will provide setup instructions.

## Authentication

Configure {{SKILL_NAME}} authentication using one of these methods:

### Option 1: Environment Variables (Recommended)

```bash
export {{SERVICE_NAME_UPPER}}_BASE_URL="https://your-instance.example.com"
export {{SERVICE_NAME_UPPER}}_TOKEN="your-api-token"
```

Add these to your `~/.bashrc` or `~/.zshrc` for persistence.

### Option 2: Config File

Create `~/.config/agent-skills/{{SERVICE_NAME}}.yaml`:

```yaml
url: https://your-instance.example.com
token: your-api-token
```

### Required Credentials

- **URL**: Your {{SKILL_NAME}} instance URL (e.g., `https://your-instance.example.com`)
- **Token**: API token or personal access token

## Commands

### check

Verify configuration and connectivity.

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py check
```

This validates:
- Python dependencies are installed
- Authentication is configured
- Can connect to {{SKILL_NAME}}

### list

List resources.

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py list
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py list --max-results 20
```

**Arguments:**
- `--max-results`: Maximum number of results (default: 50)
- `--json`: Output as JSON

### get

Get a specific resource by ID.

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py get RESOURCE_ID
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py get RESOURCE_ID --json
```

**Arguments:**
- `resource_id`: The resource identifier (required)
- `--json`: Output as JSON

### create

Create a new resource.

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py create --name "New Resource" --description "Description"
```

**Arguments:**
- `--name`: Resource name (required)
- `--description`: Resource description
- `--json`: Output as JSON

## Examples

### Verify Setup

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py check
```

### List all resources

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py list
```

### Get resource details

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py get my-resource-id
```

### Create a new resource

```bash
python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py create --name "My Resource" --description "A description"
```

## Error Handling

**Authentication errors are not retryable.** If a command fails with an authentication or permission error (exit code 1), do NOT retry the same command. Instead:

1. Inform the user about the error
2. Suggest the user verify their credentials and re-run `python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py check`
3. Authentication setup requires user interaction and cannot be completed autonomously

**Retryable errors**: Rate limiting (HTTP 429) and temporary server errors (HTTP 5xx) may succeed on retry after a brief wait. All other errors should be reported to the user.

## Model Guidance

This skill makes API calls requiring structured input/output. A standard-capability model is recommended.

## Troubleshooting

### Check command fails

Run `python $SKILL_DIR/scripts/{{SERVICE_NAME}}.py check` to diagnose issues. It will provide specific error messages and setup instructions.

### Authentication failed

1. Verify your API token is correct
2. Ensure the URL is correct
3. Check token permissions

### Permission denied

You may not have access to the requested resource. Contact your administrator.

### Import errors

Ensure dependencies are installed:
```bash
pip install --user requests keyring pyyaml
```
